# Swift/Xcode
.build/
.swiftpm/
*.xcodeproj/
DerivedData/
*.playground
xcuserdata/

# Claude Code agent worktree scratch (local; was accidentally committed as stray gitlinks)
.claude/worktrees/

# CLion/IDE
.idea/
cmake-build-*/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Audio temp files
*.wav
*.flac
*.mp3
*.tmp

# EXCEPTION: the S8.3 metadata-extraction test fixtures are committed on purpose
# (self-made/public-domain tagged audio; see Tests/Fixtures/artwork-audio/README.md).
!Tests/Fixtures/artwork-audio/*

# Compiled test binaries (built locally by Scripts/build-null-test.sh)
Tests/DSPKernelNullTest
Tests/lufs-tool
Tests/EQSafetyClampTest
# TEST-ONLY libebur128 oracle object (compiled into the harness; never committed)
Tests/.ebur128.o
# HAL bit-perfect "Pure Mode" spike — throwaway diagnostic, kept local-only (source +
# build outputs); not held to the production C++ gate, so not committed.
scripts/hal-spike*

# Generated test-harness fixtures (written at runtime into test-data/, never /tmp).
# Keep the directory (README) tracked so the path resolves on a fresh clone.
/test-data/*
!/test-data/README.md
